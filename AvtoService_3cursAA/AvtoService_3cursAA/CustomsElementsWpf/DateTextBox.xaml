<UserControl x:Class="AvtoService_3cursAA.CustomsElementsWpf.DateTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AvtoService_3cursAA.CustomsElementsWpf"
             mc:Ignorable="d" 
             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <TextBox Tag="dd.mm.yyyy"
                 x:Name="dateTextBox"
                 Style="{StaticResource ActionTextBox}"
                 PreviewKeyDown="dateTextBox_PreviewKeyDown"
                 TextChanged="dateTextBox_TextChanged"
                 PreviewTextInput="dateTextBox_PreviewTextInput"
                 MaxLength="10"/>

        <materialDesign:PackIcon Kind="Calendar"
                                 Style="{StaticResource IconsActions}"/>
    </Grid>
</UserControl>
